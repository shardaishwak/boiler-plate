import React from "react";
import styles from "./App.module.scss";

const App = props => {
    return (
        <div className={styles.App}>
            <h1>Hello world</h1>
        </div>
    );
};

export default App;
